# .profile template installed by ansible
export RAILS_ENV="production"
export RAILS_MASTER_KEY="{{ rails_master_key }}"
export PATH="/home/{{ devops_ssh_user }}/.rbenv/bin:${PATH}"
eval "$(rbenv init -)"
